defmodule Helpdesk.Support.RepresentativeTest do
  use One.DataCase, async: true
  # alias Helpdesk.Support.Representative

  # test "calculation that reaches into embedded attribute" do
  #   representative = %Representative{name: "John Doe"} |> Ash.load!(:is_admin)
  #   dbg(representative.is_admin)
  # end
end
